{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Opening and saving notebooks through the native file picker, and setting the webview zoom for readability.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:webview:allow-set-webview-zoom",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
